# Review routing contract

The provider adapter selects the cross-provider backend. Runtime configuration
may select an optional supporting scanner. Reviewer follows the installed
PR-review workflow for invocation order, required scope, result
classification, and coordinator routing.
