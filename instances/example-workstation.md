**Instance-specific setup — example workstation:** Keep device credentials and
machine-specific settings local. Do not add API tokens, private host names,
employee information, or organization-specific procedures to this profile.

Add only instructions that are safe to publish. For private machine guidance,
create an untracked `instances/<profile-name>.local.md` file and select it with
`./install.sh --instance <profile-name>.local`.
