{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "provider": {"type": "string", "enum": ["claude", "codex"]},
    "global_marker": {"type": "string"},
    "reviewer_marker": {"type": "string"},
    "workflow_marker": {"type": "string"},
    "skill_marker": {"type": "string"},
    "available_roles": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "coordinator",
          "exec-env-prepper",
          "executor",
          "reviewer",
          "educator",
          "pr-maintainer"
        ]
      },
      "minItems": 6,
      "maxItems": 6,
      "uniqueItems": true
    },
    "max_depth": {"type": "integer"},
    "educator_is_leaf": {"type": "boolean"},
    "all_operational_children_are_leaves": {"type": "boolean"},
    "canonical_state_writer": {"type": "string", "const": "coordinator"},
    "review_independence": {"type": "string", "const": "different-foundation"},
    "pr_maintainer_poll_interval_seconds": {"type": "integer", "const": 600},
    "pr_maintainer_can_message_registered_executor": {"type": "boolean"},
    "state_path": {"type": "string"}
  },
  "required": [
    "provider",
    "global_marker",
    "reviewer_marker",
    "workflow_marker",
    "skill_marker",
    "available_roles",
    "max_depth",
    "educator_is_leaf",
    "all_operational_children_are_leaves",
    "canonical_state_writer",
    "review_independence",
    "pr_maintainer_poll_interval_seconds",
    "pr_maintainer_can_message_registered_executor",
    "state_path"
  ]
}
