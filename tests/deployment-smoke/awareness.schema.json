{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "provider": {"type": "string", "enum": ["claude", "codex"]},
    "global_marker": {"type": "string"},
    "reviewer_marker": {"type": "string"},
    "workflow_marker": {"type": "string"},
    "skill_marker": {"type": "string"},
    "available_roles": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "coordinator",
          "exec-env-prepper",
          "executor",
          "reviewer",
          "pr-maintainer"
        ]
      },
      "minItems": 5,
      "maxItems": 5,
      "uniqueItems": true
    },
    "max_depth": {"type": "integer"},
    "education_is_coordinator_mode": {"type": "boolean"},
    "education_requires_explicit_entry": {"type": "boolean"},
    "education_uses_coordinator_model": {"type": "boolean"},
    "education_can_delegate_supporting_work": {"type": "boolean"},
    "education_defaults_to_no_execution_artifacts": {"type": "boolean"},
    "learner_profile_is_education_scoped": {"type": "boolean"},
    "learner_profile_update_policy": {
      "type": "string",
      "const": "ask"
    },
    "all_operational_children_are_leaves": {"type": "boolean"},
    "canonical_state_writer": {"type": "string", "const": "coordinator"},
    "review_independence": {"type": "string", "const": "different-foundation"},
    "pr_maintainer_poll_interval_seconds": {"type": "integer", "const": 600},
    "pr_maintainer_can_message_registered_executor": {"type": "boolean"},
    "state_path": {"type": "string"}
  },
  "required": [
    "provider",
    "global_marker",
    "reviewer_marker",
    "workflow_marker",
    "skill_marker",
    "available_roles",
    "max_depth",
    "education_is_coordinator_mode",
    "education_requires_explicit_entry",
    "education_uses_coordinator_model",
    "education_can_delegate_supporting_work",
    "education_defaults_to_no_execution_artifacts",
    "learner_profile_is_education_scoped",
    "learner_profile_update_policy",
    "all_operational_children_are_leaves",
    "canonical_state_writer",
    "review_independence",
    "pr_maintainer_poll_interval_seconds",
    "pr_maintainer_can_message_registered_executor",
    "state_path"
  ]
}
